# Required: Vercel Blob read/write token used by /api/blob-upload to sign
# direct-from-browser uploads, and by /api/check to write the corrected file.
# Get this from the Vercel dashboard:
#   Storage → your Blob store → ".env.local" tab → BLOB_READ_WRITE_TOKEN
BLOB_READ_WRITE_TOKEN=
